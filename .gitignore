result
result-*
.direnv

# Local agent tooling config (machine-local, not for the tracked tree)
.perles/

# Gas Town (added by gt)
.runtime/
.logs/
__pycache__/
state.json
CLAUDE.md
CLAUDE.local.md

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Gas City
.beads/*
!.beads/config.yaml
!.beads/metadata.json

# Claude Code per-session worktrees (runtime state, created by EnterWorktree)
.claude/worktrees/

# pre-commit-config.yaml is a symlink into /nix/store regenerated by the
# devShell's pre-commit hook setup. Different store paths on different
# machines.
.pre-commit-config.yaml
